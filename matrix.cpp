#include <iostream>
/*
int main() {
    double left[] = { 1, 2, 3, 4 };
    double right[] = { 3, 2, -1, 0 };
    double result[4];

    // code to make result = left * right as matrices
    for (int i = 0; i < 2; i++) {
        for (int j = 0; j < 2; j++) {
            result[i * 2 + j] = 0;
            for (int k = 0; k < 2; k++) {
                result[i * 2 + j] += left[i * 2 + k] * right[k * 2 + j];
            }
        }
    }
    // print
    for (int i = 0; i < 4; i++) {
        std::cout << result[i] << " ";
    }
    std::cout << std::endl;

    return 0;
}
*/