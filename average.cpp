/*
#include <iostream>
#include <vector>

double average(std::vector<double> nums)
{
	double total = 0.0;
	for (int i = 0; i < nums.size(); ++i)
		total = total + nums[i];
	return total / nums.size();
}
int main()
{
	std::vector<double> data = { 1, 2, 3 };
	//int n; 
	//std::cin >> n;
	//data.push_back(n);
	std::cout << "The average of our data is " << average(data) << ".\n";

	system("pause"); // I just added this part so we can see the result of the program.
}
*/