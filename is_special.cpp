/*
#include <iostream>

bool is_special(char c) {
	if ((c >= 'a' and c <= 'z') or (c >= 'A' and c <= 'Z') or (c >= '0') and (c <= '9'))
		return false;
	else
		return true;
}

int main() {
	std::cout << "Enter a special symbol. ";
	char c;
	std::cin >> c;
	if (is_special(c))
		std::cout << c << " is a special symbol.";
	else
		std::cout << c << " is not a special symbol";
}
*/