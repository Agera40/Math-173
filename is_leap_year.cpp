/*
#include <iostream>

bool is_leap_year(int year) {
	return (year % 400 == 0) or (year % 4 == 0 and year % 100 != 0);
}

int main() {
	int year; 
	std::cout << "Enter the year to know if it's is a leap: ";
	std::cin >> year;

	if (is_leap_year(year)) {
		std::cout << year << " is a leap year.";
	}
	else {
		std::cout << year << " is not a leap year. ";
	}
}
*/